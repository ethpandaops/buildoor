definitions:
  action_plan.BidPlan:
    properties:
      bid_end_time:
        type: integer
      bid_increase:
        description: gwei
        type: integer
      bid_interval:
        description: ms, >= 0, 0 = single bid
        type: integer
      bid_min_amount:
        description: gwei
        type: integer
      bid_start_time:
        type: integer
      bid_subsidy:
        description: gwei
        type: integer
      bid_value_gwei:
        description: |-
          BidValueGwei is an absolute bid base value replacing
          max(blockValue, min) + subsidy; BidIncrease still applies per re-bid.
          Allows underbidding the block value for testing.
        type: integer
      ignore_missing_prefs:
        description: |-
          IgnoreMissingPrefs bids with the payload's fee recipient when no gossip
          proposer preferences arrived for the slot, bypassing the skip gate.
        type: boolean
      mode:
        $ref: '#/definitions/action_plan.Mode'
    type: object
  action_plan.BuilderAPIPlan:
    properties:
      mode:
        $ref: '#/definitions/action_plan.Mode'
      response_delay_ms:
        description: |-
          ResponseDelayMs delays the bid response by this many milliseconds
          (context-cancellable, capped at one slot).
        type: integer
      total_value_override_gwei:
        description: |-
          TotalValueOverrideGwei is the absolute total proposer-visible bid value
          (before the Gloas execution-payment split), replacing block value +
          subsidy. May exceed the block value to test payment edge cases.
        type: integer
      value_subsidy_gwei:
        description: ValueSubsidyGwei replaces the global BlockValueSubsidyGwei for
          this slot.
        type: integer
    type: object
  action_plan.FrozenPlan:
    properties:
      bid:
        allOf:
        - $ref: '#/definitions/action_plan.ResolvedBidSettings'
        description: |-
          Resolved effective settings; a nil category = suppressed for this slot
          (by plan or by the global enable flags / protocol applicability).
      build:
        allOf:
        - $ref: '#/definitions/action_plan.ResolvedBuildSettings'
        description: |-
          Build is the complete resolved build decision for the slot (schedule +
          plan force/suppress). Always non-nil.
      builder_api:
        $ref: '#/definitions/action_plan.ResolvedBuilderAPISettings'
      fork:
        description: fork name at the target slot
        type: string
      frozen_at:
        type: string
      plan:
        allOf:
        - $ref: '#/definitions/action_plan.SlotPlan'
        description: raw sparse plan; nil = none existed
      reveal:
        $ref: '#/definitions/action_plan.ResolvedRevealSettings'
      slot:
        type: integer
    type: object
  action_plan.Mode:
    enum:
    - disabled
    - custom
    type: string
    x-enum-varnames:
    - ModeDisabled
    - ModeCustom
  action_plan.PlanUpdate:
    properties:
      bid:
        items:
          type: integer
        type: array
      builder_api:
        items:
          type: integer
        type: array
      delete:
        type: boolean
      from_slot:
        type: integer
      reveal:
        items:
          type: integer
        type: array
      set:
        additionalProperties:
          items:
            type: integer
          type: array
        type: object
      slots:
        items:
          type: integer
        type: array
      to_slot:
        type: integer
    type: object
  action_plan.ResolvedBidSettings:
    properties:
      end_ms:
        type: integer
      forced:
        description: |-
          Forced marks that the plan activated bidding although the module is
          globally disabled.
        type: boolean
      ignore_missing_prefs:
        description: IgnoreMissingPrefs bids without gossip proposer preferences.
        type: boolean
      increase_gwei:
        type: integer
      interval_ms:
        type: integer
      min_gwei:
        type: integer
      start_ms:
        type: integer
      subsidy_gwei:
        type: integer
      value_gwei:
        description: |-
          ValueGwei, when set, is the absolute bid base value replacing the
          max(blockValue, min) + subsidy formula (IncreaseGwei still applies).
        type: integer
    type: object
  action_plan.ResolvedBuildSettings:
    properties:
      build:
        description: 'Build is the final decision: build a payload for this slot or
          not.'
        type: boolean
      build_start_time_ms:
        description: |-
          BuildStartTimeMs is the effective build start time, milliseconds
          relative to slot start (signed).
        type: integer
      forced:
        description: |-
          Forced marks builds the plan pushed past the schedule (they never
          consume the next_n budget).
        type: boolean
      plan_involved:
        description: |-
          PlanInvolved marks decisions where a per-slot plan existed or any
          consumer was effectively active — i.e. skips worth surfacing.
        type: boolean
      skip_reason:
        description: |-
          SkipReason is one of the BuildSkipReason* constants when Build is
          false, empty otherwise.
        type: string
    type: object
  action_plan.ResolvedBuilderAPISettings:
    properties:
      delay_ms:
        type: integer
      forced:
        description: |-
          Forced marks that the plan activated serving although the module is
          globally disabled.
        type: boolean
      subsidy_gwei:
        type: integer
      total_value_gwei:
        description: |-
          TotalValueGwei, when set, is the absolute total proposer-visible bid
          value (before the Gloas execution-payment split).
        type: integer
    type: object
  action_plan.ResolvedRevealSettings:
    properties:
      bypass_deadline:
        description: |-
          BypassDeadline disables the "past the in-slot deadline → skip" check so
          deliberately late reveals are attempted.
        type: boolean
      reveal_time_ms:
        type: integer
      suppressed:
        type: boolean
    type: object
  action_plan.RevealPlan:
    properties:
      mode:
        $ref: '#/definitions/action_plan.Mode'
      reveal_time_ms:
        description: RevealTimeMs is signed milliseconds relative to slot start.
        type: integer
    type: object
  action_plan.SlotPlan:
    properties:
      bid:
        $ref: '#/definitions/action_plan.BidPlan'
      builder_api:
        $ref: '#/definitions/action_plan.BuilderAPIPlan'
      reveal:
        $ref: '#/definitions/action_plan.RevealPlan'
      slot:
        type: integer
      updated_at:
        type: string
      updated_by:
        type: string
    type: object
  api.ActionPlanResponse:
    properties:
      max_slot:
        type: integer
      min_slot:
        type: integer
      plans:
        items:
          $ref: '#/definitions/action_plan.SlotPlan'
        type: array
    type: object
  api.AuditLogResponse:
    properties:
      entries:
        items:
          $ref: '#/definitions/db.AuditLog'
        type: array
      limit:
        type: integer
      offset:
        type: integer
      total:
        type: integer
    type: object
  api.BidArtifactMetaEntry:
    properties:
      at:
        description: unix milliseconds
        type: integer
      execution_payment_gwei:
        type: integer
      fork:
        type: string
      index:
        type: integer
      total_value_gwei:
        type: integer
      transport:
        type: string
    type: object
  api.BidsWonResponse:
    properties:
      bids_won:
        items:
          $ref: '#/definitions/payload_bidder.WonBlock'
        type: array
      limit:
        type: integer
      offset:
        type: integer
      total:
        type: integer
    type: object
  api.BuilderAPIStatusResponse:
    properties:
      block_value_subsidy_gwei:
        type: integer
      enabled:
        type: boolean
      validator_count:
        type: integer
    type: object
  api.BuilderPreferencesEntry:
    properties:
      max_execution_payment:
        type: integer
      validator_pubkey:
        type: string
    type: object
  api.BuilderPreferencesResponse:
    properties:
      preferences:
        items:
          $ref: '#/definitions/api.BuilderPreferencesEntry'
        type: array
    type: object
  api.GetValidatorsResponse:
    properties:
      validators:
        items:
          $ref: '#/definitions/api.ValidatorRegistrationResponse'
        type: array
    type: object
  api.LifecycleStatusResponse:
    properties:
      balance_gwei:
        type: integer
      builder_index:
        type: integer
      deposit_epoch:
        type: integer
      effective_balance_gwei:
        type: integer
      is_registered:
        type: boolean
      pending_payments_gwei:
        type: integer
      withdrawable_epoch:
        type: integer
    type: object
  api.OverviewBalances:
    properties:
      cl_balance_gwei:
        type: integer
      effective_balance_gwei:
        type: integer
      pending_payments_gwei:
        type: integer
      wallet_address:
        type: string
      wallet_balance_wei:
        type: string
    type: object
  api.OverviewELClient:
    properties:
      code:
        type: string
      commit:
        type: string
      name:
        type: string
      version:
        type: string
    type: object
  api.OverviewResponse:
    properties:
      balances:
        $ref: '#/definitions/api.OverviewBalances'
      builder_index:
        type: integer
      builder_pubkey:
        type: string
      current_slot:
        type: integer
      el_client:
        $ref: '#/definitions/api.OverviewELClient'
      is_registered:
        type: boolean
      running:
        type: boolean
      services:
        $ref: '#/definitions/api.OverviewServices'
      stats:
        $ref: '#/definitions/api.OverviewStats'
      version:
        type: string
    type: object
  api.OverviewServices:
    properties:
      builder_api_available:
        type: boolean
      builder_api_enabled:
        type: boolean
      epbs_available:
        type: boolean
      epbs_enabled:
        type: boolean
      epbs_registration_state:
        type: string
      lifecycle_available:
        type: boolean
      lifecycle_enabled:
        type: boolean
    type: object
  api.OverviewStats:
    properties:
      bids_submitted:
        type: integer
      bids_won:
        type: integer
      blocks_included:
        type: integer
      builder_api_blocks_published:
        type: integer
      builder_api_headers_requested:
        type: integer
      builder_api_registered_validators:
        type: integer
      slots_built:
        type: integer
    type: object
  api.ProposerPreferencesEntry:
    properties:
      client_name:
        type: string
      fee_recipient:
        type: string
      slot:
        type: integer
      target_gas_limit:
        type: integer
      validator_index:
        type: integer
    type: object
  api.ProposerPreferencesResponse:
    properties:
      preferences:
        items:
          $ref: '#/definitions/api.ProposerPreferencesEntry'
        type: array
    type: object
  api.SlotBidArtifactsResponse:
    properties:
      bids:
        items:
          $ref: '#/definitions/api.BidArtifactMetaEntry'
        type: array
      slot:
        type: integer
    type: object
  api.SlotResultsResponse:
    properties:
      max_slot:
        type: integer
      min_slot:
        type: integer
      results:
        items:
          $ref: '#/definitions/slot_results.SlotResult'
        type: array
    type: object
  api.StatsResponse:
    properties:
      bids_submitted:
        type: integer
      bids_won:
        type: integer
      blocks_included:
        type: integer
      builder_api_blocks_published:
        type: integer
      builder_api_headers_requested:
        description: Builder API stats
        type: integer
      builder_api_registered_validators:
        type: integer
      reveals_failed:
        type: integer
      reveals_skipped:
        type: integer
      reveals_success:
        type: integer
      slots_built:
        type: integer
      total_paid_gwei:
        type: integer
    type: object
  api.StatusResponse:
    properties:
      builder_index:
        type: integer
      builder_pubkey:
        type: string
      cl_balance_gwei:
        type: integer
      current_slot:
        type: integer
      deposit_epoch:
        type: integer
      effective_balance_gwei:
        type: integer
      is_registered:
        type: boolean
      lifecycle_enabled:
        type: boolean
      pending_payments_gwei:
        type: integer
      running:
        type: boolean
      wallet_address:
        type: string
      wallet_balance_wei:
        type: string
      withdrawable_epoch:
        type: integer
    type: object
  api.UpdateActionPlanRequest:
    properties:
      updates:
        items:
          $ref: '#/definitions/action_plan.PlanUpdate'
        type: array
    type: object
  api.UpdateActionPlanResponse:
    properties:
      plans:
        items:
          $ref: '#/definitions/action_plan.SlotPlan'
        type: array
      slots:
        items:
          type: integer
        type: array
      status:
        type: string
    type: object
  api.UpdateEPBSRequest:
    properties:
      bid_end_time:
        type: integer
      bid_increase:
        type: integer
      bid_interval:
        type: integer
      bid_min_amount:
        type: integer
      bid_start_time:
        type: integer
      bid_subsidy:
        type: integer
      build_start_time:
        type: integer
      payload_build_delay:
        type: integer
      reveal_time:
        type: integer
    type: object
  api.UpdateScheduleRequest:
    properties:
      every_nth:
        type: integer
      mode:
        type: string
      next_n:
        type: integer
      start_slot:
        type: integer
    type: object
  api.ValidatorRegistrationResponse:
    description: Returns the list of validators registered via the Builder API (fee
      recipient preferences). Not paginated.
    properties:
      fee_recipient:
        description: Hex-encoded Ethereum address
        type: string
      gas_limit:
        description: Gas limit for blocks
        type: integer
      pubkey:
        description: Hex-encoded BLS public key
        type: string
      timestamp:
        description: Unix timestamp
        type: integer
    type: object
  db.AuditLog:
    properties:
      action:
        type: string
      actor:
        type: string
      detail:
        type: string
      id:
        type: integer
      remote_addr:
        type: string
      result:
        type: string
      target:
        type: string
      timestamp:
        type: integer
    type: object
  payload_bidder.WonBlock:
    properties:
      block_hash:
        type: string
      num_blobs:
        type: integer
      num_transactions:
        type: integer
      slot:
        type: integer
      source:
        type: string
      timestamp:
        description: Unix milliseconds at inclusion time
        type: integer
      value_eth:
        type: string
      value_wei:
        type: string
    type: object
  slot_results.BidAttempt:
    properties:
      artifact_index:
        description: |-
          ArtifactIndex references the slot's stored 'bid' SSZ artifact; nil when
          no artifact exists (suppressed, pre-construction failure, or capture
          disabled/failed).
        type: integer
      at:
        type: string
      competitor_high_gwei:
        description: |-
          CompetitorHighGwei is the highest known competitor bid at submission
          time (p2p only, our own builder index excluded).
        type: integer
      error:
        type: string
      execution_payment_gwei:
        type: integer
      status:
        $ref: '#/definitions/slot_results.BidStatus'
      total_value_gwei:
        type: integer
      transport:
        description: payload_builder.BidTransport values
        type: string
    type: object
  slot_results.BidStatus:
    enum:
    - suppressed
    - constructed
    - submitted
    - served
    - failed
    - cancelled
    type: string
    x-enum-varnames:
    - BidStatusSuppressed
    - BidStatusConstructed
    - BidStatusSubmitted
    - BidStatusServed
    - BidStatusFailed
    - BidStatusCancelled
  slot_results.BlockSubmission:
    properties:
      at:
        type: string
      dialect:
        description: '"legacy" | "epbs"'
        type: string
      error:
        type: string
      status:
        $ref: '#/definitions/slot_results.SubmissionStatus'
    type: object
  slot_results.BuildOutcome:
    properties:
      at:
        type: string
      block_hash:
        type: string
      block_value_wei:
        type: string
      error:
        type: string
      fee_recipient:
        type: string
      num_blobs:
        type: integer
      num_transactions:
        type: integer
      skip_reason:
        description: action_plan.BuildSkipReason* when skipped
        type: string
      status:
        $ref: '#/definitions/slot_results.BuildStatus'
    type: object
  slot_results.BuildStatus:
    enum:
    - waiting_attributes
    - no_attributes
    - started
    - ready
    - failed
    - skipped
    type: string
    x-enum-varnames:
    - BuildStatusWaitingAttributes
    - BuildStatusNoAttributes
    - BuildStatusStarted
    - BuildStatusReady
    - BuildStatusFailed
    - BuildStatusSkipped
  slot_results.InclusionResult:
    properties:
      block_hash:
        type: string
      num_blobs:
        type: integer
      num_transactions:
        type: integer
      source:
        description: '"epbs" | "builder_api"'
        type: string
      timestamp:
        type: string
      value_eth:
        type: string
      value_wei:
        type: string
    type: object
  slot_results.RevealAttempt:
    properties:
      at:
        type: string
      attempt:
        type: integer
      error:
        type: string
      skip_reason:
        description: plan_disabled | late
        type: string
      status:
        $ref: '#/definitions/slot_results.RevealStatus'
      transport:
        type: string
    type: object
  slot_results.RevealStatus:
    enum:
    - suppressed
    - published
    - failed
    - skipped
    type: string
    x-enum-varnames:
    - RevealStatusSuppressed
    - RevealStatusPublished
    - RevealStatusFailed
    - RevealStatusSkipped
  slot_results.SlotResult:
    properties:
      applied_plan:
        allOf:
        - $ref: '#/definitions/action_plan.FrozenPlan'
        description: AppliedPlan is the frozen plan snapshot the slot executed under.
      bids:
        items:
          $ref: '#/definitions/slot_results.BidAttempt'
        type: array
      block_submissions:
        items:
          $ref: '#/definitions/slot_results.BlockSubmission'
        type: array
      build:
        $ref: '#/definitions/slot_results.BuildOutcome'
      dropped_attempts:
        additionalProperties:
          type: integer
        description: |-
          DroppedAttempts counts attempts beyond the per-kind retention cap,
          keyed by kind ("bids", "block_submissions", "reveal_attempts").
        type: object
      epoch:
        type: integer
      fork:
        type: string
      inclusion:
        $ref: '#/definitions/slot_results.InclusionResult'
      reveal_attempts:
        items:
          $ref: '#/definitions/slot_results.RevealAttempt'
        type: array
      slot:
        type: integer
      updated_at:
        type: string
    type: object
  slot_results.SubmissionStatus:
    enum:
    - received
    - accepted
    - failed
    type: string
    x-enum-varnames:
    - SubmissionStatusReceived
    - SubmissionStatusAccepted
    - SubmissionStatusFailed
info:
  contact: {}
paths:
  /api/buildoor/action-plan:
    get:
      description: Returns all per-slot action plans within the inclusive slot range.
      operationId: getActionPlan
      parameters:
      - description: Range start slot (inclusive)
        in: query
        name: min_slot
        required: true
        type: integer
      - description: Range end slot (inclusive)
        in: query
        name: max_slot
        required: true
        type: integer
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/api.ActionPlanResponse'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
        "503":
          description: Plan service unavailable
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Get per-slot action plans
      tags:
      - ActionPlan
    post:
      consumes:
      - application/json
      description: |-
        Applies a bulk per-slot action plan mutation atomically (all
        targeted slots update or none). Category members are
        three-state (absent = unchanged, null = clear, object =
        replace); the "set" member supports fine-grained path updates
        like {"bid.bid_min_amount": 5000} for partial edits. Updates
        targeting past or already-frozen slots fail with 409.
        Requires authentication.
      operationId: updateActionPlan
      parameters:
      - description: Bearer token
        in: header
        name: Authorization
        required: true
        type: string
      - description: Plan updates
        in: body
        name: request
        required: true
        schema:
          $ref: '#/definitions/api.UpdateActionPlanRequest'
      produces:
      - application/json
      responses:
        "200":
          description: Authoritative normalized result
          schema:
            $ref: '#/definitions/api.UpdateActionPlanResponse'
        "400":
          description: Validation error
          schema:
            additionalProperties:
              type: string
            type: object
        "401":
          description: Unauthorized
          schema:
            additionalProperties:
              type: string
            type: object
        "409":
          description: Slot in the past or frozen
          schema:
            additionalProperties:
              type: string
            type: object
        "503":
          description: Plan service unavailable
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Update per-slot action plans
      tags:
      - ActionPlan
  /api/buildoor/audit-log:
    get:
      description: Returns a paginated list of authenticated mutating actions. Empty
        when no state-db is configured.
      operationId: getAuditLog
      parameters:
      - description: Bearer token
        in: header
        name: Authorization
        required: true
        type: string
      - default: 0
        description: Offset for pagination
        in: query
        name: offset
        type: integer
      - default: 20
        description: Limit for pagination (max 100)
        in: query
        name: limit
        type: integer
      produces:
      - application/json
      responses:
        "200":
          description: Success
          schema:
            $ref: '#/definitions/api.AuditLogResponse'
        "401":
          description: Unauthorized
          schema:
            additionalProperties:
              type: string
            type: object
        "500":
          description: Server Error
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Get the audit log
      tags:
      - Buildoor
  /api/buildoor/bids-won:
    get:
      description: Returns a paginated list of won blocks (Builder API and p2p ePBS)
        with transaction counts, blob counts, and values, read from the shared inclusion
        tracker.
      operationId: getBidsWon
      parameters:
      - default: 0
        description: Offset for pagination
        in: query
        name: offset
        type: integer
      - default: 20
        description: Limit for pagination (max 100)
        in: query
        name: limit
        type: integer
      produces:
      - application/json
      responses:
        "200":
          description: Success
          schema:
            $ref: '#/definitions/api.BidsWonResponse'
        "500":
          description: Server Error
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Get bids won (blocks of ours included on chain)
      tags:
      - Buildoor
  /api/buildoor/builder-api-status:
    get:
      description: Returns the current status of the Builder API including configuration
        and validator count.
      operationId: getBuilderAPIStatus
      produces:
      - application/json
      responses:
        "200":
          description: Success
          schema:
            $ref: '#/definitions/api.BuilderAPIStatusResponse'
        "500":
          description: Server Error
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Get Builder API status
      tags:
      - Buildoor
  /api/buildoor/builder-preferences:
    get:
      description: Returns all builder preferences currently in the cache, submitted
        by proposers via the submitBuilderPreferences API.
      operationId: getBuilderPreferences
      produces:
      - application/json
      responses:
        "200":
          description: Success
          schema:
            $ref: '#/definitions/api.BuilderPreferencesResponse'
        "404":
          description: Builder API not enabled
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Get cached builder preferences
      tags:
      - Buildoor
  /api/buildoor/overview:
    get:
      description: |-
        Returns a single-payload summary used by the multi-instance overview UI:
        running state, builder pubkey, current slot, EL client info, available/enabled
        services, balances, and recent build stats.
      operationId: getOverview
      produces:
      - application/json
      responses:
        "200":
          description: Success
          schema:
            $ref: '#/definitions/api.OverviewResponse'
      summary: Get a compact overview of this buildoor instance
      tags:
      - Buildoor
  /api/buildoor/proposer-preferences:
    get:
      description: Returns all proposer preferences currently in the cache, received
        via P2P gossip.
      operationId: getProposerPreferences
      produces:
      - application/json
      responses:
        "200":
          description: Success
          schema:
            $ref: '#/definitions/api.ProposerPreferencesResponse'
        "404":
          description: Proposer preferences not enabled
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Get cached proposer preferences
      tags:
      - Buildoor
  /api/buildoor/slot-results:
    get:
      description: |-
        Returns the recorded outcome history (build, bids, block
        submissions, reveals, inclusion, applied plan) for every
        active slot within the inclusive slot range.
      operationId: getSlotResults
      parameters:
      - description: Range start slot (inclusive)
        in: query
        name: min_slot
        required: true
        type: integer
      - description: Range end slot (inclusive)
        in: query
        name: max_slot
        required: true
        type: integer
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/api.SlotResultsResponse'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
        "503":
          description: Results tracker unavailable
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Get per-slot results
      tags:
      - ActionPlan
  /api/buildoor/slot-results/{slot}/bids:
    get:
      description: |-
        Lists the metadata of every signed bid stored for the slot
        (JSON only; fetch individual bids by index for SSZ). Returns
        an empty list for slots without bid artifacts.
      operationId: getSlotBidArtifacts
      parameters:
      - description: Slot
        in: path
        name: slot
        required: true
        type: integer
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/api.SlotBidArtifactsResponse'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
        "503":
          description: Results tracker unavailable
          schema:
            additionalProperties:
              type: string
            type: object
      summary: List a slot's signed bid artifacts
      tags:
      - ActionPlan
  /api/buildoor/slot-results/{slot}/bids/{index}:
    get:
      description: |-
        Returns one signed bid created for the slot, addressed by its
        artifact index (see the bid listing endpoint). Pre-Gloas bids
        are SignedBuilderBid, Gloas+ bids SignedExecutionPayloadBid.
        Content negotiation as with the payload artifact endpoint.
      operationId: getSlotBidArtifact
      parameters:
      - description: Slot
        in: path
        name: slot
        required: true
        type: integer
      - description: Bid artifact index
        in: path
        name: index
        required: true
        type: integer
      produces:
      - application/json
      - application/octet-stream
      responses:
        "200":
          description: Versioned signed bid (or raw SSZ)
          schema:
            additionalProperties: true
            type: object
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
        "404":
          description: No artifact for this slot/index
          schema:
            additionalProperties:
              type: string
            type: object
        "406":
          description: No acceptable content type
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Get one signed bid of a slot
      tags:
      - ActionPlan
  /api/buildoor/slot-results/{slot}/envelope:
    get:
      description: |-
        Returns the signed execution payload envelope constructed for
        the slot's reveal (stored at construction time, so failed
        publishes remain inspectable). Content negotiation as with the
        payload artifact endpoint.
      operationId: getSlotEnvelopeArtifact
      parameters:
      - description: Slot
        in: path
        name: slot
        required: true
        type: integer
      produces:
      - application/json
      - application/octet-stream
      responses:
        "200":
          description: Versioned envelope (or raw SSZ)
          schema:
            additionalProperties: true
            type: object
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
        "404":
          description: No artifact for this slot
          schema:
            additionalProperties:
              type: string
            type: object
        "406":
          description: No acceptable content type
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Get the signed payload envelope of a slot
      tags:
      - ActionPlan
  /api/buildoor/slot-results/{slot}/payload:
    get:
      description: |-
        Returns the exact execution payload built for the slot. With
        "Accept: application/octet-stream" the raw SSZ bytes are
        served; otherwise a {"version", "data"} JSON envelope. The
        Eth-Consensus-Version response header carries the fork name.
      operationId: getSlotPayloadArtifact
      parameters:
      - description: Slot
        in: path
        name: slot
        required: true
        type: integer
      produces:
      - application/json
      - application/octet-stream
      responses:
        "200":
          description: Versioned payload (or raw SSZ)
          schema:
            additionalProperties: true
            type: object
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
        "404":
          description: No artifact for this slot
          schema:
            additionalProperties:
              type: string
            type: object
        "406":
          description: No acceptable content type
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Get the built execution payload of a slot
      tags:
      - ActionPlan
  /api/buildoor/validators:
    get:
      description: Returns the list of validators registered via the Builder API (fee
        recipient preferences). Not paginated.
      operationId: getValidators
      produces:
      - application/json
      responses:
        "200":
          description: Success
          schema:
            $ref: '#/definitions/api.GetValidatorsResponse'
        "500":
          description: Server Error
          schema:
            additionalProperties:
              type: string
            type: object
      summary: List registered validators
      tags:
      - Buildoor
  /api/config:
    get:
      description: Returns the buildoor configuration in use. Sensitive fields (builder
        key, wallet key, JWT secret) are redacted.
      operationId: getConfig
      produces:
      - application/json
      responses:
        "200":
          description: Success
          schema:
            additionalProperties: true
            type: object
        "500":
          description: Server Error
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Get buildoor configuration
      tags:
      - Config
  /api/config/epbs:
    post:
      consumes:
      - application/json
      description: |-
        Updates the EPBS (enshrined PBS) configuration including timing and bid
        parameters. Requires authentication.
      operationId: updateEPBS
      parameters:
      - description: Bearer token
        in: header
        name: Authorization
        required: true
        type: string
      - description: EPBS configuration
        in: body
        name: request
        required: true
        schema:
          $ref: '#/definitions/api.UpdateEPBSRequest'
      produces:
      - application/json
      responses:
        "200":
          description: Success
          schema:
            additionalProperties:
              type: string
            type: object
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
        "401":
          description: Unauthorized
          schema:
            additionalProperties:
              type: string
            type: object
        "500":
          description: Server Error
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Update EPBS configuration
      tags:
      - Config
  /api/config/schedule:
    post:
      consumes:
      - application/json
      description: |-
        Updates the builder schedule configuration including mode, every_nth, and next_n
        settings. Requires authentication.
      operationId: updateSchedule
      parameters:
      - description: Bearer token
        in: header
        name: Authorization
        required: true
        type: string
      - description: Schedule configuration
        in: body
        name: request
        required: true
        schema:
          $ref: '#/definitions/api.UpdateScheduleRequest'
      produces:
      - application/json
      responses:
        "200":
          description: Success
          schema:
            additionalProperties:
              type: string
            type: object
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
        "401":
          description: Unauthorized
          schema:
            additionalProperties:
              type: string
            type: object
        "500":
          description: Server Error
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Update schedule configuration
      tags:
      - Config
  /api/config/settings:
    post:
      consumes:
      - application/json
      description: |-
        Applies partial global settings updates keyed by canonical
        settings paths (e.g. {"epbs.bid_subsidy": 1000,
        "schedule.mode": "all"}) without requiring full config
        objects. Unknown keys are rejected. Requires authentication.
      operationId: updateSettings
      parameters:
      - description: Bearer token
        in: header
        name: Authorization
        required: true
        type: string
      - description: Map of settings key paths to values
        in: body
        name: request
        required: true
        schema:
          type: object
      produces:
      - application/json
      responses:
        "200":
          description: Success
          schema:
            additionalProperties:
              type: string
            type: object
        "400":
          description: Unknown key or invalid value
          schema:
            additionalProperties:
              type: string
            type: object
        "401":
          description: Unauthorized
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Update global settings by key path
      tags:
      - Config
  /api/lifecycle/deposit:
    post:
      consumes:
      - application/json
      description: |-
        Initiates a builder registration deposit. If the builder is not yet
        registered, this will register it with the specified amount. Requires authentication.
      operationId: postDeposit
      parameters:
      - description: Bearer token
        in: header
        name: Authorization
        required: true
        type: string
      - description: Deposit amount in gwei
        in: body
        name: request
        required: true
        schema:
          properties:
            amount_gwei:
              type: integer
          type: object
      produces:
      - application/json
      responses:
        "200":
          description: Success
          schema:
            additionalProperties:
              type: string
            type: object
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
        "401":
          description: Unauthorized
          schema:
            additionalProperties:
              type: string
            type: object
        "404":
          description: Lifecycle management not enabled
          schema:
            additionalProperties:
              type: string
            type: object
        "500":
          description: Server Error
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Trigger builder deposit
      tags:
      - Lifecycle
  /api/lifecycle/exit:
    post:
      consumes:
      - application/json
      description: |-
        Initiates a voluntary exit for the builder. This will begin the withdrawal
        process and the builder will stop being eligible for block building after
        the exit is processed. Requires authentication.
      operationId: postExit
      parameters:
      - description: Bearer token
        in: header
        name: Authorization
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: Success
          schema:
            additionalProperties:
              type: string
            type: object
        "401":
          description: Unauthorized
          schema:
            additionalProperties:
              type: string
            type: object
        "404":
          description: Lifecycle management not enabled
          schema:
            additionalProperties:
              type: string
            type: object
        "500":
          description: Server Error
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Trigger voluntary exit
      tags:
      - Lifecycle
  /api/lifecycle/status:
    get:
      description: |-
        Returns the builder lifecycle status including registration state, balance,
        pending payments, and epoch information.
      operationId: getLifecycleStatus
      produces:
      - application/json
      responses:
        "200":
          description: Success
          schema:
            $ref: '#/definitions/api.LifecycleStatusResponse'
        "404":
          description: Lifecycle management not enabled
          schema:
            additionalProperties:
              type: string
            type: object
        "500":
          description: Server Error
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Get lifecycle status
      tags:
      - Lifecycle
  /api/lifecycle/topup:
    post:
      consumes:
      - application/json
      description: |-
        Checks the builder balance and initiates a top-up if needed based on
        configured thresholds. Requires authentication.
      operationId: postTopup
      parameters:
      - description: Bearer token
        in: header
        name: Authorization
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: Success
          schema:
            additionalProperties:
              type: string
            type: object
        "401":
          description: Unauthorized
          schema:
            additionalProperties:
              type: string
            type: object
        "404":
          description: Lifecycle management not enabled
          schema:
            additionalProperties:
              type: string
            type: object
        "500":
          description: Server Error
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Trigger balance top-up
      tags:
      - Lifecycle
  /api/stats:
    get:
      description: |-
        Returns builder statistics including slots built, bids submitted/won,
        total paid, and reveal success/failure counts.
      operationId: getStats
      produces:
      - application/json
      responses:
        "200":
          description: Success
          schema:
            $ref: '#/definitions/api.StatsResponse'
        "500":
          description: Server Error
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Get builder statistics
      tags:
      - Stats
  /api/status:
    get:
      description: |-
        Returns the current builder status including running state, current slot,
        builder index and public key.
      operationId: getStatus
      produces:
      - application/json
      responses:
        "200":
          description: Success
          schema:
            $ref: '#/definitions/api.StatusResponse'
        "500":
          description: Server Error
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Get builder status
      tags:
      - Status
  /api/version:
    get:
      description: Returns the current version
      operationId: getVersion
      produces:
      - application/json
      responses:
        "200":
          description: Success
          schema:
            type: string
        "500":
          description: Server Error
          schema:
            type: string
      summary: Get the current version
      tags:
      - Version
swagger: "2.0"
