# Git and IDE
.git
.gitignore
.vscode
.cursor
*.swp
.DS_Store

# Build artifacts
bin/
buildoor

# Config and logs (mount at runtime)
config.yaml
test-config.yaml
config-*.yaml
log-*
tmp-*
temp/

# Devnet and scripts (not needed in image)
.hack/
*.sh

# Frontend dev (node_modules excluded, but source files needed for build)
pkg/webui/node_modules

# Tests and coverage (smaller image; keep validators/testdata/ - it is embedded in code)
*_test.go
*.test
coverage.out

# Docs and misc
*.md
!README.md
