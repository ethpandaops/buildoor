# Git and IDE
.git
.gitignore
.vscode
.cursor
*.swp
.DS_Store

# Config and logs (mount at runtime)
config.yaml
test-config.yaml
config-*.yaml
log-*
tmp-*
temp/

# Devnet and scripts (not needed in image)
.hack/
*.sh

# Frontend: exclude only node_modules (source and config needed for Docker build)
pkg/webui/node_modules

# Tests and coverage (smaller image; keep validators/testdata/ - it is embedded in code)
*_test.go
*.test
coverage.out

# Docs and misc
*.md
!README.md
